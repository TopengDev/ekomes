// "use client"

// import {useState} from 'react'

// export const FilterButtons = ({children, filter,fetchFunction}: { filter: any,fetchFunction: any} & React.PropsWithChildren) => {

//    const [filter, setFilter] = useState<any>({})

//    return (
//       <div className='flex item-center gap-4'>
//          {Object.keys(filter).map((filter) => (
//             <ToggleButton active={true}>{filter}</ToggleButton>

//          ))}
//             <ToggleButton active={false}>Women</ToggleButton>
//             <ToggleButton active={false}>Unisex</ToggleButton>
//             <ToggleButton active={false}>Boys</ToggleButton>
//             <ToggleButton active={false}>Girls</ToggleButton>
//          </div>
//    )
// }
